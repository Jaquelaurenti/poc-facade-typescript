class PaymentProcessor {
  public verifyPayment(paymentInfo: any): boolean {
    // Lógica para verificar o status do pagamento
    console.log('Verificando pagamento:', paymentInfo);
    return true;
  }
}

export default PaymentProcessor;
